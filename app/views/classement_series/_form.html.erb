<%= form_for(classement_series) do |f| %>
  <% if classement_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classement_series.errors.count, "error") %> prohibited this classement_series from being saved:</h2>

      <ul>
      <% classement_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %>
    <%= f.text_area :nom %>
  </div>

  <div class="field">
    <%= f.label :genre %>
    <%= f.text_area :genre %>
  </div>

  <div class="field">
    <%= f.label :synopsis %>
    <%= f.text_area :synopsis %>
  </div>

  <div class="field"> 
    <%= f.label :note %>
     
      <input type="radio"> 
      <input type="radio">
      <input type="radio">
      <input type="radio">
      <input type="radio">
   

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
